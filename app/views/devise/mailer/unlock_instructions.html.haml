%p
  = "#{t('mailers.devise.headers.hello')} #{@resource.email}"
  \!
%p= t('account_has_been_locked')
%p= t('click_the_link_to_unlock')
%p= link_to t('unlock_my_account'), unlock_url(@resource, unlock_token: @token)
